<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
  <h1 class="h2"><%= t('dashboard') %></h1>
</div>

<div class="grid">

  <%= render partial: 'bodies_count' %>
  <%= render partial: 'stars_summary' %>
  <%= render partial: 'habitable_candidates' %>
  <%= render partial: 'top_5_system_mass' %>
  <%= render partial: 'habitable_zones' %>
  <%= render partial: 'moons_per_star' %>
  <%= render partial: 'closest_systems_summary' %>

</div>
